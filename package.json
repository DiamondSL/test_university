{
  "name": "university_project",
  "version": "0.0.1",
  "description": "",
  "keywords": [
    "university",
    "firebase",
    "student"
  ],
  "license": "",
  "author": "",
  "scripts": {
    "build": "parcel build index.html *.html",
    "dev": "parcel *.html index.html --open",
    "dev_firebase": "parcel *.html --open",
    "start": "npm run build && npm run dev",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "express": "^4.18.2",
    "firebase": "^9.21.0",
    "parcel": "^2.8.3"
  },
  "devDependencies": {
    "@parcel/packager-raw-url": "^2.8.3",
    "@parcel/transformer-webmanifest": "^2.8.3",
    "process": "^0.11.10"
  }
}
